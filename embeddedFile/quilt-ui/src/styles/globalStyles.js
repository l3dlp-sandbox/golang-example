import * as colors from './colors'
import * as typography from './typography'

const commonStyles = {
    shadow: {
        boxShadow: `0px 0px 8px ${colors.general02Color}40`
    }
}

export {colors, typography, commonStyles}